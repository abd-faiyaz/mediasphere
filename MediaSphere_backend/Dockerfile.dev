# MediaSphere Backend Development Dockerfile

# Use the same base image as the build stage in the production Dockerfile
FROM maven:3.9.6-eclipse-temurin-21-alpine

# Set working directory
WORKDIR /app

# Copy pom.xml and source code
COPY pom.xml .
COPY src ./src

# Install wget for healthcheck (if not already present in base image)
RUN apk add --no-cache wget

# Expose port
EXPOSE 8080

# Command to run the application in development mode with live-reloading
RUN apk add --no-cache bash


